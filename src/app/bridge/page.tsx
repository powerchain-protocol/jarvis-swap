import { ArrowLeftRight, Info, ShieldCheck } from "lucide-react";
import styles from "@/components/shared/market.module.css";

export default function BridgePage(){return <div className="center-card"><div className="page-header"><div><h1 className="section-title">JARVIS Bridge</h1><p className="section-subtitle">Move JARVIS between its canonical Sui origin and supported bridged networks.</p></div></div><section className={`card ${styles.bridgeCard}`}><div className={styles.bridgeRoute}><Network letter="S" title="Sui" description="Canonical JARVIS"/><div className={styles.routeArrow}><ArrowLeftRight size={18}/></div><Network letter="◎" title="Solana" description="Bridged JARVIS representation"/></div><div className={styles.bridgeNotice}><ShieldCheck size={17}/><span><strong>Canonical supply rule.</strong> JARVIS is canonical on Sui. The Solana asset is a bridged representation and must never be described as native JARVIS.</span></div><div className={styles.bridgeNotice}><Info size={17}/><span>Bridge execution is intentionally unavailable in this UI scaffold until audited lock/mint and burn/unlock adapters, replay protection, finality checks, and recovery handling are configured.</span></div><button className="button-primary" style={{width:"100%",marginTop:16}} disabled>Bridge — adapter required</button></section></div>}
function Network({letter,title,description}:{letter:string;title:string;description:string}){return <div className={styles.networkCard}><div className={styles.networkTop}><span className={styles.networkBadge}>{letter}</span><strong>{title}</strong></div><p>{description}</p></div>}
