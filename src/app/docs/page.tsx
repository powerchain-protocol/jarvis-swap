export default function DocsPage(){return <div className="center-card"><div className="page-header"><div><h1 className="section-title">JARVIS Swap Docs</h1><p className="section-subtitle">Production boundaries for wallets, quotes, transaction execution, custom tokens, and bridge safety.</p></div></div><section className="card" style={{padding:22}} id="security"><h2 style={{marginTop:0}}>Production wiring</h2><p className="muted">Live execution uses server-validated Cetus routing. Production builds do not ship a mock quote fallback. Signed transaction bytes are simulated through Sui gRPC, signature-verified, re-simulated immediately before submission, and then executed through the server submission boundary.</p><h3>Fail-closed execution</h3><p className="muted">Execution is fail-closed: a failed quote check, expired quote, failed simulation, invalid signature, fee-policy mismatch, minimum-output breach, or failed finality check is never presented as a successful swap.</p><h3>Quote validation</h3><p className="muted">Never treat client-computed output, price, route, slippage, or deadline values as authoritative. Revalidate the sender, network, coin types, exact amount in, minimum amount out, route, quote expiry, and transaction bytes on the trusted execution boundary.</p><h3>Custom-token safety</h3><p className="muted">Metadata returned by Sui RPC is descriptive data, not a trust signal. User-imported tokens remain explicitly unverified until a separate trusted registry recognizes the exact coin type.</p><h3>Bridge semantics</h3><p className="muted">JARVIS is canonical on Sui. A Solana representation must be described as bridged JARVIS. Production bridging must enforce supply invariants, replay protection, finality, source/destination validation, and recovery for incomplete transfers.</p><h3>Secrets</h3><p className="muted">Keep authenticated RPC URLs, aggregator API keys, bridge credentials, and signing material server-side. Do not place secrets in variables prefixed with NEXT_PUBLIC_.</p></section></div>}
